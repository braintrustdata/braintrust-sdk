[project]
name = "examples"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11.10"
dependencies = [
    "braintrust-langchain",
    "langchain>=0.3.27",
]

[tool.uv.sources]
braintrust-langchain = { workspace = true }
