import * as adapter from "./adapter";

/**
 * @deprecated Use the AI SDK integration exported by `braintrust` instead. This package will stop being published after the next release.
 */
export const BraintrustAdapter = adapter;
