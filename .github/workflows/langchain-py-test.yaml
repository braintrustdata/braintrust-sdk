name: langchain-py

on:
  workflow_call:
    inputs:
      python-version:
        required: true
        type: string
      os:
        required: true
        type: string

jobs:
  test:
    runs-on: ${{ inputs.os }}

    defaults:
      run:
        working-directory: integrations/langchain-py

    env:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
          cache-dependency-glob: |
            integrations/langchain-py/pyproject.toml
            integrations/langchain-py/uv.lock

      - name: Install dependencies
        run: |
          uv python install ${{ inputs.python-version }}
          uv sync

      - name: Lint with ruff
        if: ${{ inputs.os == 'ubuntu-latest' }}
        run: |
          uv run ruff check $(git ls-files '*.py' | grep -v 'examples/')

      - name: Run tests
        run: |
          uv run pytest src

      - name: Test import
        run: |
          uv run python -c "import braintrust_langchain; print('braintrust_langchain imported successfully')"
          uv run python -c "from braintrust_langchain import BraintrustTracer; print('BraintrustTracer imported successfully')"
