# Changelog

Release notes can be found [here](https://www.braintrust.dev/docs/reference/release-notes).

## [Unreleased]

### Changed

- **Browser**: Integrated `als-browser` for AsyncLocalStorage support in browser environments
- **Browser**: Parent-child span relationships now work automatically in all browser environments
- **Browser**: `currentSpan()` and nested `traced()` calls now function correctly in browsers
