export class Completions {
  constructor(client) {
    this._client = client;
  }

  async parse(params) {
    return this._client.post('/beta/chat/completions/parse', params);
  }

  stream(params) {
    return this._client.stream('/beta/chat/completions', params);
  }
}
