name: Test SDK with Zod Versions
on:
  pull_request:
    paths:
      - "sdk/js/**"
      - "sdk/package.json"
      - ".github/workflows/test-sdk-zod-versions.yaml"
  push:
    branches:
      - main
    paths:
      - "sdk/js/**"
      - "sdk/package.json"
  workflow_dispatch:

jobs:
  test-zod-versions:
    name: Test with Zod ${{ matrix.zod-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        zod-version: ["3.25.34", "4.2.1"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8.15.5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"
          cache-dependency-path: "sdk/pnpm-lock.yaml"

      - name: Install dependencies
        working-directory: sdk/js
        run: pnpm install --frozen-lockfile

      - name: Install Zod ${{ matrix.zod-version }}
        working-directory: sdk/js
        run: pnpm add -D zod@${{ matrix.zod-version }}

      - name: Verify Zod version
        working-directory: sdk/js
        run: |
          echo "Installed Zod version:"
          pnpm list zod

      - name: Run type checking
        working-directory: sdk/js
        run: pnpm tsc --noEmit

      - name: Run zod compatibility tests
        working-directory: sdk/js
        run: pnpm test src/zod-compat.test.ts

      - name: Run zod v3 serialization tests
        if: matrix.zod-version == '3.25.34'
        working-directory: sdk/js
        run: pnpm test src/zod-serialization.test.ts util/zod_util.test.ts

      - name: Run zod v4 serialization tests
        if: matrix.zod-version == '4.2.1'
        working-directory: sdk/js
        run: pnpm test src/zod-v4-serialization.test.ts util/zod_util.test.ts

      - name: Run parameter tests
        working-directory: sdk/js
        run: pnpm test src/parameters.test.ts

      - name: Run all tests (excluding wrappers and smoke tests)
        working-directory: sdk/js
        run: pnpm test

  test-summary:
    name: Test Summary
    needs: test-zod-versions
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Check test results
        run: |
          if [ "${{ needs.test-zod-versions.result }}" != "success" ]; then
            echo "❌ Tests failed with one or more Zod versions"
            exit 1
          else
            echo "✅ All tests passed with both Zod 3.x and 4.x"
          fi
